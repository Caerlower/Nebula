{
  "name": "nebula",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "build": "pnpm --filter nebula-frontend build",
    "dev": "pnpm --filter nebula-frontend dev",
    "vercel:configure": "node scripts/configure-vercel.mjs"
  }
}

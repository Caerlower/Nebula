{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "tanstack-start",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter nebula-frontend build && mkdir -p .vercel && cp -R apps/web/.vercel/output .vercel/output"
}

/**
 * Empty stand-in for the optional `sodium-native` binding required by
 * @stellar/stellar-sdk. The SDK falls back to its pure-JS implementation
 * when the native module is unavailable, which is always the case in the
 * browser bundle.
 */
export default {};
